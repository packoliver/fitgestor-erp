/**
 * DEPRECADO. Use setup-sandbox.ts (orquestrador com fases + manifesto + credenciais seguras).
 */
console.error(
  "\n⚠ scripts/sandbox/seed-sandbox.ts foi substituído por setup-sandbox.ts.\n" +
    "  Execute:  bun scripts/sandbox/setup-sandbox.ts\n"
);
process.exit(2);
