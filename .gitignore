# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Environment variables (contains API keys/secrets — never commit)
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Sandbox runtime artifacts (Fatia 2.1b)
scripts/sandbox/.sandbox-manifest.json
scripts/sandbox/.sandbox-credentials.json

.vercel
.audit-artifacts/

# Notas internas de continuidade entre sessões de IA — não publicar em repositório público
CONTINUIDADE-CODEX.md
CONTINUIDADE-CLAUDE.md
SHOPIFY-AUDIT-STATUS.md

# Estado local do Supabase CLI (ref do projeto, versões, URL do pooler).
# Sem senha, mas é estado de máquina, não do projeto.
supabase/.temp/

# Config local do Claude Code (dev server, permissões da sessão)
.claude/

# Documento de trabalho do usuário, não faz parte do código
RESUMO-PARA-ANALISE-EXTERNA.md

# Documento de repasse gerado para envio externo
HANDOFF-13-09-2026.md

# Documento de repasse gerado para envio externo
REPASSE-CODEX.md
